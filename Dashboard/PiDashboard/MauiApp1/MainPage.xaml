<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             xmlns:viewmodels="clr-namespace:MauiApp1.ViewModels">

    <ContentPage.BindingContext>
        <viewmodels:DevicesViewModel/>
    </ContentPage.BindingContext>

    <VerticalStackLayout 
        HorizontalOptions="Fill" >
        <CollectionView ItemsSource="{Binding Devices}" HorizontalOptions="Fill">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout HorizontalOptions="Fill" Padding="15,5,0,0" Spacing="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Text="{Binding name}" FontSize="10" TextColor="Black" HorizontalOptions="Start"/>
                            <Label Grid.Column="1" Text="{Binding status}" FontSize="10" TextColor="{Binding color}" HorizontalOptions="End"/>
                        </Grid>
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <Button Text="Add Device" Clicked="OnAddDeviceClicked" />
    </VerticalStackLayout>

</ContentPage>
